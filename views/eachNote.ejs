<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= note.name %> - My Notes</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #f0f2f5;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    header {
      background: #343a40;
      color: white;
      padding: 1rem 2rem;
      text-align: center;
    }

    .container {
      max-width: 800px;
      margin: 2rem auto;
      padding: 1rem 2rem;
      background: white;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      animation: fadeIn 0.5s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 1rem;
      color: #333;
    }

    p {
      font-size: 1.05rem;
      line-height: 1.6;
      color: #555;
      white-space: pre-wrap;
    }

    .button-group {
      margin-top: 2rem;
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
    }

    .back-button,
    .delete-button,
    .edit-button {
      padding: 10px 20px;
      border-radius: 6px;
      text-decoration: none;
      font-weight: 500;
      transition: background 0.2s ease;
      text-align: center;
    }

    .back-button {
      background: #007bff;
      color: white;
    }

    .back-button:hover {
      background: #0056b3;
    }

    .edit-button {
      background: #ffc107;
      color: #212529;
    }

    .edit-button:hover {
      background: #e0a800;
    }

    .delete-button {
      background: #dc3545;
      color: white;
      border: none;
      cursor: pointer;
    }

    .delete-button:hover {
      background: #b02a37;
    }

    @media (max-width: 600px) {
      .container {
        margin: 1rem;
        padding: 1rem;
      }

      h1 {
        font-size: 1.5rem;
      }

      .button-group {
        flex-direction: column;
        gap: 0.5rem;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>View Note</h1>
  </header>

  <div class="container">
    <h1><%= note.name %></h1>
    <p><%= note.content %></p>

    <div class="button-group">
      <a class="back-button" href="/">← Back to Notes</a>

      <a class="edit-button" href="/notes/<%= note._id %>/edit">✏️ Edit</a>

      <form method="POST" action="/notes/<%= note._id %>?_method=DELETE" onsubmit="return confirm('Are you sure you want to delete this note?');">
        <button class="delete-button" type="submit">🗑️ Delete</button>
      </form>
    </div>
  </div>
</body>
</html>
